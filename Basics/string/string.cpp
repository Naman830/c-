#include <bits/stdc++.h>
using namespace std;

int main(){
    string s = "Hello World";
    // we check length of string using size()
    int len = s.size();
    // last index n-1
    cout << s[len - 1] << endl;
    return 0;
}